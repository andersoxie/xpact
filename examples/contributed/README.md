# Contributed examples

This folder contains contributed xpact incremental examples and a matching C
expat tag-counter helper. They are staged here for source control and Jenkins
smoke execution, but they are not part of the published benchmark
documentation.

The Jenkins wrapper is `scripts/run_contributed_examples.ps1`.
